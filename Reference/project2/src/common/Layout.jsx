import Footer from './Footer';
import Header from './Header';

const Layout = () => {
    return (
        <div className="wrap">
            <Header />

            <Footer />
        </div>
    );
};

export default Layout;
